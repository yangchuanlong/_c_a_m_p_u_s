<!--index.wxml-->
<view class="container" bindtouchstart="onTouchStart" bindtouchend="onTouchEnd">
    <view class='searchWrapper'>
      <input confirm-type='search' 
      bindinput='inputSearch'
      bindconfirm='startSearch'
      placeholder='请输入搜索内容'
      ></input>
    </view>
    <view class='tabs' bindtap='onTabClick'>
        <view wx:for="{{tabs}}" class="{{activeTab == index ? 'active' : ''}}" data-tab-index='{{index}}'>
            {{item}}
        </view>
    </view>
   <view class="sectionWrapper">
       <view id="all" class="section" style="display: {{activeTab == 0 ? '' : 'none'}}">
           <view wx:for="{{questions}}" class="item" wx:key="{{item._id}}" data-id="{{item._id}}" bindtap='gotoDetail'>
               <view class="author">
                   <image src="{{item.avatar}}" class="avatar" wx:if="{{item.avatar}}" ></image>
                   <image wx:else src="../images/userIcon.png" class="avatar" style="background:gray"></image>
                   <text class="nickName">{{item.nickName}}</text>
                   <text class="date">{{item.formatedTime}}</text>
               </view>
               <view class="title">{{item.title}}</view>
               <view class="shortContent">{{item.shortContent}}</view>
               <view class="imagesWrapper" wx:if="{{item.images.length}}">
                   <image wx:for="{{item.images}}" wx:for-item="imgItem" wx:key="{{imgItem}}" src="{{imgItem}}"></image>
               </view>
               <view class='actions'>
                   <view>
                       <image wx:if="{{myThumbups[item._id]}}" src="../images/thumbup.png" id='js-thumbup-cancel'></image>
                       <image wx:else src="../images/not-thumbup.png" id='js-thumbup'></image>
                   </view>
                   <view> 赞({{item.thumbupCount}})</view>
                   <view>|</view>
                   <view>回答({{item.replyCount}})</view>
               </view>
           </view>
       </view>
       <view id="hotSearch" class="section" style="display: {{activeTab == 1 ? '' : 'none'}}">
           <view style='padding:12rpx'>热搜栏目, 开发中...</view>
       </view>
   </view>

    <view class="icons">
        <image src="../images/icon.jpg" class="logo"></image>
        <image src="../images/edit.png" class="editIcon" bindtap='gotoAsk'></image>
        <image src="../images/userIcon.png" class="userIcon" bindtap='gotoMine'></image>
        <image src="../images/message.png" class="message" bindtap='gotoMsg'></image>
    </view>
    <view class='loading' style='display:{{showLoading ? "" : "none"}}'>
      <image src='../images/loading.gif'></image>
    </view>
</view>
